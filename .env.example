# ─── SCATO - Environment Configuration ───
# Copy this file to .env and fill in values as needed.
# All variables are OPTIONAL — SCATO works with zero configuration.

# ─── Web Dashboard Port ───
SCATO_PORT=3001

# ─── Data Directory ───
# Where scan history and cache are stored (default: ~/.scato)
# SCATO_DATA_DIR=/path/to/custom/data

# ─── External APIs (Optional — improves scan quality) ───

# NVD API Key — increases rate limits for NVD lookups
# Get one free at: https://nvd.nist.gov/developers/request-an-api-key
NVD_API_KEY=

# GitHub Token — enables GHSA advisory data and PR integration
# Get from: https://github.com/settings/tokens (no special scopes needed)
GITHUB_TOKEN=
